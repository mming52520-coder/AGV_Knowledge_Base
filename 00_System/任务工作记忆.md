---
id: "agv:system:task-work-memory"
type: log
status: active
review: generated
project: AGV
updated: 2026-09-24
tags: [AGV, 工作记忆]
---

# 知识库任务工作记忆

本仓库未发现另一份知识库本地任务工作记忆；外部源码工作记忆只作为来源路径登记，不复制进本库。后续任务按阶段在此追加经核对的事实、决定、验证和阻塞项。

## 2026-09-24：分支治理与双视图基础

- 基线：main a072378b54583aa12bcd383eb36489863e88c861；现有标签 kg-code-v1-e8a1b3d、kg-code-v2-aef23b8c-wt-03fcd3118087。
- 桌面主工作区已有 Obsidian 配置与接口笔记未提交修改，故使用独立任务 worktree。
- 当前仓库为 Public；负责人本次再次明确要求保持 Public。仓库无 LICENSE，规范随该决定更新。
- current.json 当前指向 V1；V2 为 candidate_pending_review。历史快照、差异、冻结工具包与指针不改写。
- 冻结 Tools 的 MANIFEST.sha256 已逐项核对通过；材料图谱基线测试因两个外部资料根目录缺失而无法通过，V2 与投影测试在隔离临时目录运行。
- 本次索引复用既有文档 id、逻辑 UID、快照实例和 current 指针，只新增可再生检索视图及一篇证据边界明确的双超声样板。

### 本轮验证与待办

- 新索引单元测试：9 项通过；全库可索引 654 篇带 frontmatter id 的笔记。重复 id、非法实例键、悬空关系、候选版本误用、路径迁移、证据过期、保护路径篡改、标签缺失与关键链接均有对应检查。
- V2 旧测试：32 项通过。Obsidian 投影旧测试：共 8 项，7 项通过、1 项按既有条件跳过。资料图谱旧测试：2 项因 Windows 外部资料根目录不存在而报错；没有修改冻结生成器来绕过输入。
- 索引 build 与 check 均通过；已人工查询“左侧双超声循墙”及带 V2 快照、车型过滤的 left_wall_node。后者仍明确返回 candidate。
- GitHub 规则集列表为空，main 的旧式 Branch Protection 返回 404；本任务只提供待应用说明，不声称保护已生效。
- 原始 ROS 源码、双左侧传感器安装/标定和实车测试证据不在当前知识库内；样板保持待复核。Obsidian 图形界面未运行，视觉验收待人工完成。
- 任务分支 feature/kb-human-ai-foundation 已推送，并创建 Draft PR #1。PR 与 push 的 Knowledge base checks 工作流均完成且 success；实际检查名为 knowledge-check。线上主线规则仍未应用，main 未合并。

## 2026-09-24：Draft PR #1 合并前收口

- 远端复核起点：base 仍为 main `a072378b54583aa12bcd383eb36489863e88c861`，PR #1 仍为 Draft，分支原 head 为 `c3a33283fbda0682b0fa43f8b028633a3a79bbc7`；原 head 的 knowledge-check 为 success。新提交必须以自身的 Actions 结果为准。
- Public 策略的授权证据来自本任务用户此前的明确消息“我选择只公开”及“现在要求仍保持public即可”，适用范围是此仓库当前保持 Public；不是由 GitHub 当前可见性反推的授权，也不等于添加开源许可证或永久放弃未来调整权。可见性变更仍须负责人明确决定；Public 分支没有保密隔离能力。冻结工具包的旧 Private 文字保留原字节。
- 分类根因：旧索引把 66 篇 `03_ROS` 稳定逻辑页标为 `current_reference`，容易误作当前源码实现。现改为 `logical_reference`；快照页由 current 指针判定为 `current_snapshot`、`candidate` 或 `historical`。候选与历史页即使按精确 id 查询，默认也不放行；未来指针变化的测试不改仓库 `current.json`。
- 索引存储决定：原 `index.json` 为 546,303 字节的单行文件。build/check/query 现统一使用 `manifest.json`、按 id 稳定排序的 `documents.jsonl` 和按端点及关系名稳定排序的 `relations.jsonl`；废弃旧文件，未引入数据库、第三方 Python 包或外部 API。manifest 记录数量及文件 SHA-256，check 比对实际字节，query 读取校验后的磁盘文件。重复 build 的字节一致性纳入单元测试和 CI。
- 本地验证：AI Index 12 项、V2 snapshot 32 项通过；Obsidian projection 共 8 项，7 项通过、1 项按既有条件跳过；冻结工具包 MANIFEST 9 项匹配。索引 check 通过，覆盖 654 篇文档及关键 Wiki 链接；中文专题、V2 `left_wall_node` 与默认候选过滤查询符合边界。最终提交与新 head 的 Actions 结果待记录在 PR 描述。
- 资料图谱旧测试依赖 `D:\推料车项目\9.车辆学习资料` 与 `D:\ROS工程文件` 等外部 Windows 目录；本机缺失，状态为 `NOT_RUN_EXTERNAL_INPUT_UNAVAILABLE`，不改冻结工具绕过。Obsidian GUI、原始 ROS 源码和实车证据仍待人工验收。
